import React from 'react';

interface PromotionNoticeBannerProps {
  compact?: boolean;
}

export const PromotionNoticeBanner: React.FC<PromotionNoticeBannerProps> = () => {
  // Promotion is officially OVER. The timer and banner have been permanently removed.
  return null;
};
